/** @type {import('tailwindcss').Config} */
module.exports = {
  //Indicamos onde o tailwind deverá procurar ficheiros html com as classes para ele compilar. Estamos dizendo que será no mesmo nivel onde temos este ficheiro de configuração.
  content: ['./*.html'],
  theme: {
    extend: {},
  },
  plugins: [],
}
